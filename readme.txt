% ReplitのRunから実行できないため、Shellからコマンドで実行
% [共通]ディレクトリを移動
~/FizzBuzzPython$ cd src
  % mainの実行
  ~/FizzBuzzPython/src$ python app/main.py
  % テストの実行
  ~/FizzBuzzPython/src$ python -m unittest discover test -v